# netlify.toml

# Build settings
[build]
    command = "cd frontend && npm run build"
    publish = "./frontend/dist"

# Redirects and rewrites
[[redirects]]
    from = "/*"
    to = "/frontend/index.html"
    status = 200



